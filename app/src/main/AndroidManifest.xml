<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.antiwaste">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntiWaste"
        android:usesCleartextTraffic="true">

        <activity android:name=".RegisterActivityCommercant"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity android:name=".LoginActivityCommercant"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />

        <activity android:name=".ActivityModifierMotDePasse"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />

        <activity
            android:name=".ActivityModifierInfoPerso"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity
            android:name=".Activity_Commercant"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity
            android:name=".Activity_Panier"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity
            android:name=".Activity_Accueil"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCC2ITpC2emZTa5w-ZpZSV_iofO5bvrAOM" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" /> <!-- Meta data for your file for server url -->
        <meta-data
            android:name="com.parse.SERVER_URL"
            android:value="@string/back4app_server_url" /> <!-- Meta data for your file for application id -->
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/back4app_app_id" /> <!-- Meta data for your file for client key -->
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/back4app_client_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AntiWaste.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>